model:
  provider: openrouter
  default: deepseek/deepseek-v4-flash

unauthorized_dm_behavior: pair

display:
  tool_progress: new

mcp_servers:
  jarvis:
    command: "PYTHON"
    args: ["-m", "app.mcp_jarvis"]
    cwd: "REPO"
    enabled: true
