# ============================================================
#  J.A.R.V.I.S. — configuración
#
#  No hace falta tocar este archivo a mano: se rellena desde
#  la propia aplicación, en AJUSTES → CLAVES Y SERVICIOS.
# ============================================================

# ---------- Cerebro (obligatorio para el chat) ----------
# Consigue una clave gratis en https://openrouter.ai/keys
# La misma clave sirve para el chat y para la visión.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://openrouter.ai/api/v1
DEEPSEEK_MODEL=deepseek/deepseek-v4-flash

# Precios del modelo (USD por millón de tokens) para el contador de gasto
PRICE_INPUT=0.064
PRICE_OUTPUT=0.129
PRICE_CACHE=0.0028

# ---------- Visión (opcional: la cámara describe lo que ve) ----------
# Misma clave de OpenRouter que arriba
OPENROUTER_API_KEY=
JARVIS_VISION_MODEL=google/gemma-4-31b-it:free

# ---------- Voz: Fish Audio ----------
# La voz de JARVIS se genera en la nube: responde en menos de un segundo,
# no necesita GPU y no hay que descargar nada.
# Consigue tu clave en https://fish.audio/app/developers
FISH_API_KEY=
# Voz por defecto de JARVIS. Para otra, busca en https://fish.audio/discovery
FISH_VOICE_ID=5bb94403e2a44a0fb0ae2829e97cdeda
# s2.1-pro-free es gratuito; s2.1-pro necesita saldo de API
FISH_MODEL=s2.1-pro-free

# Voz de reserva si Fish Audio no responde (Microsoft, necesita internet)
JARVIS_FALLBACK_VOICE=es-ES-AlvaroNeural
JARVIS_VOICE_CHAT_WHOLE=0

# ---------- El tiempo ----------
# Coordenadas de tu ciudad. Cámbialas en Ajustes o en el tutorial.
JARVIS_LAT=40.4168
JARVIS_LON=-3.7038

# ---------- Spotify (opcional) ----------
JARVIS_AUTH_FILE=auth.json
